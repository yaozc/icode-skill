{
  "project_id": "<project_id>",
  "device_config": {
    "deploy_enabled": false,
    "note": "安装本 SKILL 后，复制本模板到 ~/.claude/icode_data/device_config/<project_id>.json 并填写真实值。**<project_id> 必须等于 `basename(git rev-parse --show-toplevel)`**（在 git 仓库内运行时；不区分子目录，不得用 ticket_id 前缀或外层目录别名）。**文件名与文件内 project_id 字段必须一致**，否则 /icode patch --listen 读不到并给出缺失诊断。deploy_enabled=false 时 patch 跳过实机部署验证（可选）。deploy/log 均为配置项，由用户按工程实际情况填写。支持单个连接或多个连接，patch 验证时按 name 选用。**若 deploy 意图引用编译规范（如「参考 LIMIT 文档编译命令规范」），AI 部署前会先 Read LIMIT 对应红线按规范编译；deploy 无编译指令且 patch 涉及代码修改则按 04_code 探测规则，无需在本字段写编译命令**。**deploy 意图含「重启设备/reboot」时：AI 会在重启前记录设备指纹基线（序列号/产品 ID 与物理网卡 MAC 为主标识——排除虚拟网卡如 VMware/KVM/docker OUI；hostname 仅辅助展示），重启后轮询重连（每 25~35s 试连，上限约 180s），连上后按硬件唯一标识（序列号/物理 MAC）核对——一致才确认同一台设备并开始监听；连错（非目标设备，即使 hostname/虚拟网卡一致）则输出提示并继续重试（用户可在超时窗口内切换连接），多设备同网段时重启后 IP 可能落到任一台，AI 必须按硬件唯一标识核对并等待用户切换，不得因 hostname/虚拟网卡一致而误判，也不得用非目标设备的 uptime/进程/boot 日志时间戳判定目标设备状态；持续到超时仍未连到目标设备才停止并记录——无需在本字段写重连/校验命令**。**deploy 意图中的多步骤描述 = 步骤即执行顺序，AI 必须逐字按序展开为命令并顺序执行，禁止重排 / 合并 / 省略**（例：意图写「先停止进程再替换文件」，必须先执行停止脚本、确认进程退出后再替换，替换完成后确认可执行权限）；每步替换后须 **核对产物一致性**（本地产物 vs 设备 md5 对比，无法 md5 时用 strings 验证关键字符串），一致才进下一步；出现 `Text file busy` / 状态矛盾 → 停下重读 deploy 意图核对，不猜不跳过。",
    "connections": [
      {
        "name": "adb_main",
        "type": "adb",
        "serial": "<设备序列号>",
        "deploy": "adb -s <serial> push <artifact> <target>",
        "log": "adb -s <serial> logcat"
      },
      {
        "name": "ssh_main",
        "type": "ssh",
        "host": "<ip>",
        "port": 22,
        "user": "<user>",
        "password": "",
        "deploy": "把 <artifact> 部署到 <target> 并重启对应进程（无重启步骤可省略）",
        "log": "查看 <log_dir> 目录（含多层子目录）下新增的日志",
        "log_note": "deploy/log 用自然语言描述意图，AI 结合本连接的 host/port/user 组装实际 scp/ssh 命令——地址/密码等连接信息只在连接字段填一次，不在 deploy/log 重复。ssh 日志常含多层子目录，AI 用递归 find + marker 做增量（marker 放日志目录外，如 /tmp/.icode_marker，避免被 find 算作日志），每轮 touch 后只取新增/变更文件；追加式单文件可改用 tail"
      },
      {
        "name": "serial_debug",
        "type": "serial",
        "port": "/dev/ttyUSB0",
        "baud": 115200,
        "deploy": "",
        "log": "minicom -D <port> -b <baud>"
      }
    ]
  }
}
