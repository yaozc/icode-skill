# 步骤 6 — 终极终审 + 出具报告 + 统一修复

**Codex 调用**: `$icodex audit`（兼容：`$icodex stage=06_audit`）
**产出**: `{ICODE_OUT_DIR}/06_audit.md` + `{ICODE_OUT_DIR}/06_fixes.log`
**会话**: 主会话

## 前置校验

检查 `{ICODE_OUT_DIR}/03_plan_final.md` 和步骤4创建的代码文件是否存在，缺失则报错。

## 代码新鲜度

**开始前必须重新读取所有代码文件**。步骤 5 的修复已落盘，审计必须基于最新代码。

## 6.1 出具终审报告

1. 检测最新目录，确定 `ICODE_OUT_DIR`
2. 读取 `03_plan_final.md` 和 `.ico_metadata.json` 的 `code_files` 列表
3. 额外读取 `05_review_rounds.json`（若存在）+ `05_reverse.json`（若存在）
4. **强制思考前置**（不可跳过，缺证据视为不合规）：先输出 `ultrathink` 触发词；再完成结构化思考——**首选**调用 `sequential-thinking` MCP（至少 3 步），**MCP 不可用时降级**为输出 `### 结构化思考` 文字块（逐项完成，不可省略）；每步/每项对应一个子项：构建追溯矩阵（计划功能点→代码位置）→ 汇步骤历史 → 规划 6 维度审计策略
5. 输出：`▶ 步骤6 终审开始`
6. **重新读取所有代码文件**
7. 执行终审

### 审核维度（6个，全部覆盖）

1. **实施完整度** — 计划所有功能点 100% 落地，每个功能点必须给出代码证据位置
2. **执行精准度** — 实现与计划一致，偏差处必须指出（文件+行号）
3. **方案偏离度** — 偏离项必须明确列出
4. **代码质量** — 可读性、性能、安全性
5. **跨文件一致性** — 接口变更全链路同步，上下游数据结构对齐
6. **残留风险** — 已知或潜在问题

### 执行流程

建立追溯矩阵（逐条对照计划功能点/接口/约束，标记代码对应位置）→ 基于矩阵逐维度评分 → 对照校验项逐条勾对 → 汇总问题清单 → 给结论

### 报告格式

总体评分（百分制）+ 每维度独立评分评语 + 问题汇总清单（含位置、严重程度）+ 结论（通过/有条件通过/不通过）

写入 `{ICODE_OUT_DIR}/06_audit.md`。

参考步骤 5 深检历史，避免重复报告已修复问题。专注深检未覆盖的角度或更深层架构问题。

### 反偷懒机制

必须先建立计划-代码追溯矩阵，再逐维度评分。禁止跳过追溯直接写"全部通过"。

## 6.2 强制修复

1. 读取 `06_audit.md` 中的问题清单
2. 按严重程度排序（高 → 中 → 低）逐个修复
   - 每个问题用 Edit 工具修复，**禁止删除现有注释**
   - 修复后追加记录到 `{ICODE_OUT_DIR}/06_fixes.log`
3. 全部修复后做全局编译验证，最多 3 次
4. 更新 `.ico_metadata.json`：`status = completed`
5. 输出交付总结

## 6.3 最终交付

```text
=== ICode 工作流完成 ===
[✓] 步骤1: 计划拟定 — 完成
[✓] 步骤2: 计划审查 — 完成
[✓] 步骤3: 计划定稿 — 完成
[✓] 步骤4: 编码实施 — 完成
[✓] 步骤5: 三阶段复检 — 总 {deepcheck_total_rounds} 轮
[✓] 步骤6: 终极终审 — 评分{分数}, {结论}
产出目录: {ICODE_OUT_DIR}/
```

## 6.4 文档化（必需，合并在本步骤内执行）

**目标**：基于本次流程的最终产物（计划 + 代码 + 修复历史），**智能识别内容类型**并生成**面向人**的项目说明文档。

**产出**：`{ICODE_OUT_DIR}/README.md`（覆盖式写入，每次审计最新内容）

### 强制思考前置

**强制思考前置**（不可跳过，缺证据视为不合规）：先输出 `ultrathink` 触发词；再完成结构化思考——**首选**调用 `sequential-thinking` MCP（至少 4 步），**MCP 不可用时降级**为输出 `### 结构化思考` 文字块（逐项完成，不可省略）：通读计划要点 → 扫描代码公共接口 → 识别文档类型 → 规划章节结构。

### 文档化执行流程

1. **重新读取所有上下文**（基于终审通过后的最新状态）：
   - `03_plan_final.md`：提取功能需求、ADR、接口约束
   - `.ico_metadata.json` 的 `code_files`：列出本次涉及的文件
   - **重新读取所有代码文件**（关键 public 接口必须从代码实际扫描，不能凭计划脑补）
   - `06_audit.md`：了解修复历史，避免遗漏修复内容
2. **智能识别文档类型**（按以下优先级识别，可多选）：

   | 信号 | 文档类型 | 必须章节 |
   |------|----------|----------|
   | 新增/修改 public 类、函数、宏 | **API 参考** | 接口清单、参数说明、返回值、调用示例 |
   | 新增/修改 CLI 命令、入口脚本 | **使用手册** | 命令语法、参数列表、使用示例 |
   | 新增/修改配置文件 schema | **配置说明** | 配置项清单、默认值、取值范围 |
   | 跨模块/跨层架构调整 | **架构说明** | 模块划分、数据流、依赖关系 |
   | 涉及部署/构建脚本 | **运维说明** | 构建步骤、依赖、部署命令 |
   | 仅内部重构，无对外变化 | **变更说明** | 改动点列表、影响范围 |

3. **生成 README.md**（按需选用上述章节，**不要塞无关章节**）：

   ```markdown
   # {项目标题} — 本次变更说明

   > 本文档由 `$icodex audit` 步骤 6.4 自动生成于 {timestamp}。基于本次流程的最终产物（计划 + 代码 + 修复历史）智能识别内容类型，仅包含与本次变更相关的章节。

   ## 1. 变更概述
   {一句话概括本次做了什么 + 为什么做 + 影响范围}

   ## 2. 新增/变更功能
   {从 03_plan_final.md 的功能需求段提取，逐条列出}
   - {功能点1}：{说明}
   - {功能点2}：{说明}

   ## 3. API 变更（如适用）
   ### 新增
   - `{file}:{line}` — {接口签名} — {用途说明}
   ### 修改
   - `{file}:{line}` — {旧签名} → {新签名} — {差异说明}
   ### 删除（如有）
   - {被删除的接口} — {替代方案}

   ## 4. 使用方法（如适用）
   {命令、参数、示例；从 CLI 入口或 sample 代码提取}

   ## 5. 配置变更（如适用）
   - 新增配置项：`{key}` = {default} — {含义}
   - 修改配置项：`{key}` = {old} → {new} — {原因}

   ## 6. 架构变更（如适用）
   {模块图、数据流、依赖关系，必要时用 ASCII 图}

   ## 7. 升级注意（如有破坏性变更）
   {从旧版本升级到本版本需要做的修改}

   ## 8. 验证方法
   {如何验证本次变更生效——编译命令、测试命令、运行时检查点}

   ## 9. 已知限制
   {从 06_audit.md 残留风险段提取未消除的风险}
   ```

4. **反偷懒机制**：
   - **禁止泛泛而谈**：每条说明必须基于具体代码位置（文件:行号）或计划章节引用
   - **禁止重复 03_plan_final.md**：README.md 是面向**使用者**的，不是计划副本
   - **禁止遗漏公共接口**：必须扫描所有 `code_files` 的 public 符号
   - **章节按需生成**：如果本次无架构变更，就不写"架构变更"章节（不要凑数）

5. **完成标志**：
   - 文件已写入 `{ICODE_OUT_DIR}/README.md`
   - 章节数 ≥ 3（变更概述 + 新增/变更功能 + 至少一个按需章节）
   - 至少引用了 1 个具体代码位置（文件:行号）
   - 输出：`▶ 步骤6.4 文档化完成 — {ICODE_OUT_DIR}/README.md`

### 与现有项目 README 的关系

**不修改项目根目录的 README.md**——icode 输出目录内的 README.md 是**本次变更的归档**，与项目长期 README 共存。两者职责不同：

- 项目根 `README.md`：长期项目介绍（已有）
- `{ICODE_OUT_DIR}/README.md`：本次变更说明（本次新增）
