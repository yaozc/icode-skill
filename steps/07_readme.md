# 步骤 7 — 交付报告生成（可选，手动触发）

> **Codex 持久化前置**：执行本步骤前必须完整读取 [references/codex_runtime.md](../references/codex_runtime.md)；路径、状态、锁、合并读取、迁移和 artifact_map 与旧文字冲突时以该文件和 `~/.codex/skills/icodex/tools/icode_state.py` 为准。

**命令**: `$icodex readme`
**产出**: 每次调用**一次性生成两份**：

1. `{ICODE_OUT_DIR}/{工程简名}_{需求关键词}.md` —— **交付报告**（面向开发/了解工程的人，自包含技术总结）
2. `{ICODE_OUT_DIR}/{工程简名}_{需求关键词}_brief.md` —— **跨领域简报**（面向其它模块的研发/测试/产品，含必要改动/修复代码，比交付报告简略）

**会话**: 主会话
**定位**: **可选后置步骤，不参与步骤1~6流程推进**。步骤6完成后用户手动触发，生成两份——**交付报告给自己看**（完整技术档案）+ **跨领域简报给其它模块同事看**（含必要代码、较简略）。不改 status（仍 `completed`），不改 completed_steps（不追加"7"）。

## 前置校验

检查 `{ICODE_OUT_DIR}/.ico_metadata.json` 的 `status == "completed"`（步骤6已完成）。若未完成则报错提示先执行步骤6。

随后按 `artifact_map` 读取实际存在的 `requirement/root_cause/plan/final_plan/implementation/deepcheck/audit/patches`，不得假设 staged 编号文件存在；这使 concise full mode 与 staged mode 共用本步骤。两份报告分别写临时草稿并用 `publish-artifact --key delivery_report`、`publish-artifact --key delivery_brief` 发布，最后记录 `delivery_report_generated=true` 并运行 `validate`。任一发布失败时不设置对应映射、不宣称生成完成。

**产物时效校验（防 README 基于过时产物）**：读 metadata 的 `created_at`（工单创建时间，近似步骤6 完成的下界）+ `code_files` 列表，用 `find {code_files} -newer {ICODE_OUT_DIR}/.ico_metadata.json` 检测是否有源码文件 mtime 晚于 metadata 创建时间。若有 → 输出警告「⚠️ 检测到步骤6 完成后代码仍有变更（N 个文件 mtime 晚于终审时间），README 的"已知限制/方案"可能基于过时产物，建议重跑步骤5/6 或确认变更不影响交付」；不阻塞生成（README 的"使用说明"段含实时运行输出兜底，面向人可判断）。**该校验同时覆盖跨领域简报**（简报的"核心代码/时间点"同样基于产物，过时产物会传导到两份）。

## 文件名生成

从 metadata 的 `requirement` 提炼关键词，生成**一对文件名**（两份同步生成）：
- 工程简名：`project_path` 的 basename
- 需求关键词：从 requirement 提取核心动词+对象（如"增加取模和幂运算"→`modulo_power`，"溢出修复"→`overflow_fix`）
- 全小写 + 下划线分隔
- 交付报告：`{工程简名}_{需求关键词}.md`（示例：`{工程简名}_modulo_power.md`、`{工程简名}_overflow_fix.md`）
- 跨领域简报：`{工程简名}_{需求关键词}_brief.md`（同 basename + `_brief` 后缀，两份成对、便于查找）

## 智能模板选择

读 metadata 的 `completed_steps`，若含 `"log"` → **查BUG模板**，否则 → **功能开发模板**。**该选择只决定交付报告模板**；跨领域简报有独立模板（见「跨领域说明模板」），**不走功能/查BUG 选择**、统一用"本次变更"统领。

> **模板选择与 `"0"` 的关系**：`completed_steps` 含 `"0"`（走过 init 入口）**不影响**模板选择——init 是需求初稿，不是 BUG 修复入口。只有 `log`（日志根因分析）才是 BUG 修复入口。方式A/B/C 都走功能开发模板（含/不含 0 都行），方式D 走查BUG模板。

### 功能开发模板（方式A/B/C）

```markdown
# {需求标题} — 交付报告

**代码仓库**：`{project_path 绝对路径}`（如 `/home/user/myproject`）

**错误码体系**（首次提到错误码时列出，方便不熟悉本工程头文件的读者）：
- `{ERR_OK}` (0)：成功
- `{ERR_DIV_ZERO}` (1)：除零
- `{ERR_INVALID}` (2)：参数错
- `{ERR_OVERFLOW}` (3)：整数溢出
- `{ERR_SYNTAX}` (4)：语法错误（新增）

## 1. 需求概述
{原始需求 + 背景目标 + 与现有函数的差异点（一句话）。从 00_init.md/命令行参数提取，自包含不引用内部文件}

**与现有函数的差异点**（必填，避免与历史 readme 内容雷同）：
{一句话说明"新函数与本工程现有函数的本质差异"，如：
- 新 lcm 复用现有 gcd 而非重写 GCD
- 新 eval 是首个字符串解析入口
- 新 isqrt 与现有 sqrt 等价但走统一前缀命名}

## 2. 设计方案
{架构设计 + 关键接口签名。从 03_plan_final.md 提取要点，自包含}

### 核心接口
```c
{函数签名}
```

| 参数 | 含义 |
|------|------|
| ... | ... |

**返回值**：{错误码表，含每个返回码的触发场景}

## 3. 代码变更
{改了哪些文件 + 每个文件改了什么 + 调用链。从 code_files + 03_plan_final.md 提取，自包含}

**调用链流程图**（ASCII，必含）：
```
{入口} → {函数A} → {函数B} → {返回}
```
用 ASCII 箭头画出从入口到返回的完整调用路径，让读者一眼看懂数据流。

## 4. 安全性与测试覆盖（替代原"审查与复检"）
{从"第三人视角"重写，让读者快速判断"这函数安全吗"：
- 安全性分析：哪些边界场景已测试？哪些可能漏掉？
- 测试覆盖：测试用例覆盖了哪些场景？遗漏了哪些？
- 关键风险：残余风险与触发条件}

## 5. 实现要点（替代原"偏差与修复"，无实质偏差时也必填）
{从第三人视角重写：
- 修复前/后代码对比（如果有）
- 实现亮点（巧妙设计/复用）
- 与计划的实质偏差（如有）
- 无实质偏差时：写"本次实施与定稿计划无实质偏差"+ 一句话说明"计划设计完整，编码一次通过"}

## 6. 使用说明
{如何编译/运行/验证 + 接口调用示例 + **实际运行输出**（必含 5-10 行）。从 Makefile + 代码实际扫描 + 实测结果，自包含}

**实际运行输出**（必含，5-10 行实际 stdout/stderr）：
```
$ ./{demo可执行文件}
{func_a(12, 8) = 4 (rc=0)
func_a(0, 0) = 0 (rc=0)
...}
```

**测试用例位置**（第三人能找到测试代码）：
`{测试入口文件}` 末尾，可用 `grep "{函数名}" {测试入口文件}` 定位

## 7. 已知限制
{残留风险 + 待验证假设 + 不在本次修复范围的相关问题。从 06_audit.md 提取，自包含}

**不在本次修复范围的相关问题**（如有，如 INT_MIN 路径已通过 parse_factor 防御但字面量 -2147483648 仍会触发 OVERFLOW，**避免与已修复 BUG 重复**）
```

> **第三人视角优化要点**（避免常见问题）：
> - **缺实际运行输出**：6 使用说明段必含 5-10 行实测 stdout
> - **缺错误码体系说明**：开头列错误码表（首次提到时）
> - **"审查与复检"对第三人无价值**：改写为"安全性与测试覆盖"
> - **"偏差与修复"无内容**：重命名为"实现要点"，无偏差时写亮点
> - **新功能 readme 内容雷同**：1 需求概述必含"与现有函数的差异点"一句话
> - **BUG 修复 readme 文件名无原函数关联**：文件名用 `{原函数}_{bug 类型}.md`（如 `{工程简名}_parse_number_overflow_fix.md`）

### 查BUG模板（方式D，含 log 步骤）

```markdown
# {BUG标题} — 交付报告

**代码仓库**：`{project_path 绝对路径}`
**受影响函数**：`{原函数名}`（如 `parse_number` 是 `eval` 的子函数）

**错误码体系**（首次提到时列出）：
- `{ERR_OK}` (0)：成功
- `{ERR_DIV_ZERO}` (1)：除零
- `{ERR_INVALID}` (2)：参数错
- `{ERR_OVERFLOW}` (3)：整数溢出
- `{ERR_SYNTAX}` (4)：语法错误

## 1. BUG概述
{症状 + 影响 + 日志来源 + 触发条件（一句话）。从 log_analysis.md 提取，自包含}

**症状反差**（必填，对比突出）：
- **预期**：{正确行为 + 错误码}
- **实际**：{实际行为 + 错误码 + 错误结果值}

## 2. 根因分析
{根因假设 + 对抗验证 + 决定性证据。从 log_analysis.md 提取，自包含}

**决定性证据**（必填，必含代码行号 + 实际运行对比）：
- 代码位置：file:line（如 `src/module.c:380`）
- 实际运行：{实测值 vs 预期值，明确反差}

## 3. 修复方案
{改了什么 + 为什么这么改 + 方案对比（A/B/C 选哪个 + 拒绝其他方案的理由）。从 03_plan_final.md + 06_audit.md 提取，自包含}

**方案对比**（必填）：
| 方案 | 优点 | 缺点 | 决策 |
|------|------|------|------|
| A. {选择} | ... | ... | ✅ 采用 |
| B. {备选} | ... | ... | ❌ 拒绝（理由） |

## 4. 代码变更
{文件:行号 + 修复前/后代码对比（必填）+ 调用链。从 code_files + 代码扫描，自包含}

**修复前/后代码对比**（必填）：
```c
// 修复前（{file}:{line}）
{有 bug 的代码}

// 修复后
{修复后的代码}
```

**修复前后调用链对比**（ASCII，必含）：
```
修复前：{入口} → {函数A} → {漏洞点} → {错误返回}
修复后：{入口} → {函数A} → {新增防御} → {正确返回}
```

## 5. 安全性与测试覆盖（替代原"审查与复检"）
{从第三人视角重写：
- 安全性分析：哪些边界场景已测试？
- 测试覆盖：测试用例矩阵（含 INT_MAX/INT_MIN 边界 + 修复前/后对比）
- 关键风险：残余风险}

## 6. 验证方法
{如何确认 BUG 已修复 + 实际运行输出（必含 5-10 行）。从 main.c 测试 + 运行输出，自包含}

**实际运行输出**（必填，5-10 行实测 stdout）：
```
$ ./{demo可执行文件}
{eval("2147483647") = 2147483647 (rc=0, expect INT_MAX OK)
eval("2147483648") rc=3 (expect 3 OVERFLOW)
...}
```

**回归测试**（必填，确认没破坏其他用例）：
{原 N 个用例 + 新增 M 个用例，全部通过}

## 7. 已知限制（避免与已修复 BUG 重复）
{残余风险 + 待验证 + 不在本次修复范围的相关问题。从 06_audit.md 提取，自包含}

**不在本次修复范围的相关问题**（必填，避免与 BUG 概述重复）：
{如"INT_MIN 字面量 `-2147483648` 仍会触发 OVERFLOW（已在解析入口防御，但本 BUG 修复未涉及该路径）"}
```

### 跨领域说明模板（简报，面向其它模块的研发/测试/产品——**不区分功能/查BUG**，统一用"本次变更"统领）

```markdown
# {需求标题} — 简报（面向其它模块同事）

> **一句话总结**：{1~2 句讲清"哪个模块发生了什么 / 改了什么 / 结果如何"}

## 背景与主要问题
{其它模块同事不熟本模块——先一句话交代本模块背景，再讲现象与影响（影响谁/多严重），可含模块名与相关功能名}

## 本次需求
{目标 + **影响范围**：涉及哪些模块/接口？其它模块是否受影响或需配合？}

## 关键时间点
{从 `created_at` / `patch_history.at` / log 分析记录**真实提取**，无记录写"未记录"，时间格式 YYYY-MM-DD}
- 查BUG：发现问题（{来源：日志分析 / 用户反馈 / 测试发现}）→ 定位原因 → 完成修复 → 验证通过
- 功能开发：需求提出 → 开发完成 → 验证通过
（按本次实际类型填，不存在的环节省略或写"不涉及"）

## 变更链路
{改动涉及的代码链路（模块 → 关键文件 → 关键函数），给到其它模块同事能看懂改动落点与影响面的**必要层级**即可，不画交付报告级全量调用链图}

## 核心代码（本次改动/修复）
{列出本次改动/修复的**核心代码片段**（标注 file:line），供其它模块同事快速判断是否影响自己——给关键片段即可，不逐文件全贴、不附完整接口参数表/错误码表}

## 效果与验证
{最终效果 + 验证概况 + **其它模块需注意的兼容/调用变化**（接口签名变化、行为变化等，如有；无则写"无"）}
```

**与交付报告的分工**：交付报告是**给自己看**的完整技术档案（接口签名/参数表/错误码表/全量调用链/测试矩阵/已知限制全展开）；本简报是**给其它模块同事看**的适度版——**保留必要的改动/修复代码与链路**，省去交付报告的完整表格与全量展开。

## 自包含约束（重要）

**全文不得出现 icode 内部文件名和术语**——所有信息从产物中**提取后用通俗语言重写**。读者不需要知道 icode 是什么，不需要翻其他文件。**注意：工程源码文件名/函数名（如 `src/module.c`、`parse_number`）不是 icode 内部术语，予以保留**——简报读者要靠它们判断影响面；需规避的只是 icode 流程产物名（如 `01_plan.md`）与流程术语（如下方列表）。

**禁止出现的内部术语**（必须替换为通俗表述）：

- ❌ `01_plan.md`/`03_plan_final.md`/`review_round_*.json`/`05_deepcheck.md`/`06_audit.md`/`02_review.md`/`00_init.md`/`log_analysis.md` 等文件名 → ✅ 直接写内容
- ❌ `Agent ID`/`agentId`/`spawn`/`子代理` → ✅ "独立审查者"
- ❌ `质疑者`/`证据质疑者`/`替代解释者`/`充分性质疑者` → ✅ "独立审查者"
- ❌ `confirmed`/`refuted`/`needs_more_evidence` → ✅ "确认/推翻/待验证"
- ❌ `clean`/`clean_rounds`/`收敛` → ✅ "无新问题/审查通过"
- ❌ `has_new_issues`/`pending_verification` → ✅ "发现问题/待验证项"
- ❌ `code_deviations`/`adversarial_verification` → ✅ "实现偏差/对抗验证"
- ❌ `步骤2`/`步骤5`/`步骤6` 等步骤编号 → ✅ "审查阶段/复检阶段/终审阶段"

## 强制思考前置

**必须按 [references/thinking_core.md](../references/thinking_core.md)「强制思考前置·统一契约」段执行三件套 Read**。本步骤子项（至少5步）= 通读产物要点 → 识别报告类型(log/功能) → 提炼自包含内容 → 规划章节结构 → **精简为其它模块视角的简报内容（保留本次改动/修复的必要代码片段与链路，省去交付报告完整展开，提炼真实时间点）**。

## 执行流程

1. 检测最新目录，确定 `ICODE_OUT_DIR`
2. 读取 `.ico_metadata.json`：确认 `status==completed` + 提取 `requirement` + `completed_steps` + `code_files` + `created_at`
3. 读取所有产物（`00_init.md`/`log_analysis.md`/`01_plan.md`/`02_review.md`/`03_plan_final.md`/`05_deepcheck.md`/`06_audit.md`，**存在时含 `08_patch.md`**——补丁演进与 `patch_history.at` 时间点）+ 代码文件 + Makefile
4. 强制思考前置（见上方）
5. **生成文件名**：从 `requirement` 提炼关键词 + 工程简名 → 一对（交付报告 + `_brief` 简报）
6. **选择模板**：`completed_steps` 含 `"log"` → 查BUG模板，否则 → 功能开发模板
7. **逐章提取要点**（自包含，不引用内部文件名）
8. 使用 Write 工具写入 `{ICODE_OUT_DIR}/{工程简名}_{需求关键词}.md`（交付报告）
9. **生成跨领域简报**：把步骤7 提炼的要点**精简为其它模块视角**——保留本次改动/修复的**必要代码片段**与变更链路，省去交付报告的完整接口表/错误码表/测试矩阵/全量调用链图，按「跨领域说明模板」用 Write 写入 `{ICODE_OUT_DIR}/{工程简名}_{需求关键词}_brief.md`——关键时间点从 `created_at` / `patch_history.at` / log 分析记录**真实提取**，不编造
10. 输出：`▶ 步骤7 交付报告生成完成 — {ICODE_OUT_DIR}/{工程简名}_{需求关键词}.md（含跨领域简报 _brief）`

## 反偷懒机制

- **禁止泛泛而谈**：每条说明必须基于具体代码位置（文件:行号）或具体产物内容
- **禁止引用内部文件**：全文不得出现 icode 内部文件名
- **禁止省略章节**：交付报告 7 章节 / 跨领域简报 6 章节，各自全部有实质内容，不足处写"无"而非省略
- **禁止复制产物全文**：提取要点而非复制全文（如 ADR 只写决策+理由，不写上下文/选项）
- **简报代码要精简**：`_brief.md` 含本次改动/修复的**必要代码片段**（标注 file:line），但不复制交付报告的完整接口参数表/错误码表/测试矩阵/全量调用链图——给其它模块同事判断影响面够用即可
- **简报禁止编造时间点**：关键时间点必须来自 `created_at` / `patch_history.at` / log 分析记录，无记录写"未记录"
- **简报链路给必要层级**：链路写到"模块 → 关键文件 → 关键函数"即可，不画交付报告级全量调用链图

## 完成标志

- **两份文件均已写入**：`{ICODE_OUT_DIR}/{工程简名}_{需求关键词}.md` + `{ICODE_OUT_DIR}/{工程简名}_{需求关键词}_brief.md`
- 交付报告：7 章节全有实质内容 + 全文无 icode 内部文件名 + 至少引用 1 个具体代码位置（文件:行号）
- 跨领域简报：章节全有实质内容 + 含本次改动/修复核心代码片段（标注 file:line）+ 链路给到必要层级 + 时间点真实（无记录写"未记录"）
- 输出确认行

## 可重复执行

用户可多次 `$icodex readme` 覆盖更新（每次重新提取最新产物内容，**两份同步刷新**）。

**patch 联动**：工单经过 `$icodex patch` 追加修改后，若功能/修复范围有变化，提示用户"补丁后建议重新 `$icodex readme` 刷新交付报告 + 跨领域简报"（不自动执行，用户决定）。重新生成时须把 `08_patch.md` 的补丁演进（最新 Patch N）纳入「代码变更 / 验证方法」章节（简报的"时间点/修复效果"随之更新）。

## MCP 推荐（强证据二元化）
| MCP | 推荐级别 | 用途 |
|-----|----------|------|
| vision-bridge | 🟢* | 附加 UI 截图--用户给图时 |
| **cheap-research** | 🟢* | **降本**：generate_filename（文件名，含 `_brief` 双名）+ select_template（功能/查BUG模板选择）+ fill_template（交付报告 + 简报段落填充）+ retrieve_similar（已知限制检索防重复）。不接管决策：风险章节提炼走主会话 |
| 其他 3 个 | ⚪ | 本步骤不推荐 |

**强制约束**：🟢/🟢*/⚪ 语义 + 双保险机制（执行步骤内嵌 + thinking_core gate）详见 [SKILL.md「MCP 调用覆盖强制化」](../SKILL.md) + [references/mcp_per_step.md「双保险机制」](../references/mcp_per_step.md)；本步骤表内的 🟢/🟢* 标注按上方真源判定。
