# 反偷懒约束（共享规则）

> 本文件是 icode 所有步骤共享的「反偷懒约束」。SKILL.md 不再重复本段，各 step 执行前**必须 Read 本文件完整内容**（同会话已读不豁免，Read 后输出 `📖 已 Read references/anti_laziness.md` 确认行）。

## 硬性总则

**禁止以"省 token / 省时间 / 简化产出 / 已大致足够"为由偷工减料**。任何步骤的产出必须**完整**完成本步骤定义的所有子项、维度、轮次和文件，**不得合并、不得跳过、不得降级**。

## 典型偷懒行为（一经发现，该步骤产出视为不合规）

1. **合并产出**：把多个独立步骤的产物揉进同一个文件（如把审查和定稿合到一个文件、把代码和复检合并写）
2. **跳过子项 / 省略产物文件**：步骤定义里要求的子项/维度/章节漏写或写"略"、"同上"、"参考前文"；**步骤明确要求的 `.json` / `.md` / `.log` 等产物文件缺失或内容是空模板**（漏写文件=漏子项，合并到本条）
3. **降低轮次**：步骤 2 审查、步骤 5 复检要求的轮次未达标就提前终止（如 `max_rounds=3` 只跑 1 轮就声称"已无新问题"）
4. **示意/伪代码替代实现**：步骤 4 编码时用 `// TODO`、`// ...`、`// 省略类似实现` 等占位符代替真实代码
5. **审查/复检走过场**：审查意见全是"无问题"、"通过"等空泛结论，无具体证据
6. **跳过强制思考**：未输出 `ultrathink` 触发词、未做 MCP 思考或降级文字思考块（详见 [thinking_core.md](../references/thinking_core.md) + [thinking_detail.md](../references/thinking_detail.md)）
7. **跳过自检报告**：修改后未输出完整的 7 维自检报告
8. **跳过对抗验证**：步骤 2.5 逐维审查产出的 issue 未经独立质疑者子代理对抗就下 `confirmed` 结论（**步骤 2.4 已用 Read/Grep 实证验证失败的 issue 例外**，已有铁证可直接 `confirmed` 无需对抗）；或把主代理推理过程喂给质疑者当既定事实（破坏独立性）；或对抗未收敛时和稀泥默认通过（详见 [adversarial.md](adversarial.md)）。**质疑者必须用 Agent 工具独立 spawn，不得主代理自演**——产物（review_round_*.json 的 adversarial_verification）必须记录每个质疑者的**独立 Agent 调用证据**（spawn 的 agent 返回内容摘要），无 spawn 证据=自演=不合规。**子代理失败时禁用自演兜底**：质疑者返回空/截断/无 verdict 时，按 adversarial.md「子代理失败处理」重试 2 次（含 1 次换 subagent_type）→仍失败诚实降级为 `[未验证-子代理对抗失败]` 计入 `pending_verification`，**绝不改由主代理自演裁决、绝不伪造 confirmed**（自演=既当运动员又当裁判，确认偏误倾向确认自己结论，对抗形同虚设）。主代理 Read/Grep 实证铁证属事实核查不算自演，判断性结论才必须独立 spawn
9. **伪造共识 / 默认通过**：issue 无 `evidence_pointer` 证据回指就确认；断言无法实证验证时不标 `[未验证-证据不足]` 而默认通过；对抗裁决分裂时不降级而强行 `confirmed`
10. **跳过历史检索 / 污染产物**：`$icodex init`/`$icodex plan`/`$icodex run`/`$icodex log` 未执行历史检索注入（全局索引存在时）；或把历史参考内容写进 `00_init.md`/堆砌进 `01_plan.md`（污染工程产物）；或命中工单后照抄历史决策而不顾当前需求差异（检索规则见 SKILL.md「历史检索复用」段）。**`00_init.md` 全文（含对话摘要段）不得出现任何历史工单引用**（如"参考 demo-1 ADR-1"），历史参考只进会话上下文
11. **跳过偏差回写 / 污染计划**：步骤6 终审未把实质偏差汇总回写到 `03_plan_final.md` 的「实现偏差备忘」段（含无偏差时未写"无实质偏差"留痕）；或回写时改了计划正文（破坏对照价值）；或把细节差异堆砌进备忘（抬高回读噪音）；或步骤4 主动偏离计划未记入 `code_deviations`（偏差回写规范见 [06_audit.md](../steps/06_audit.md)）
12. **注释/日志/风格/优雅偷工**：步骤4 编码漏写导出函数/关键分支/数据结构注释；关键路径（错误返回/状态跳转/外部交互/决策分支/降级重试）漏加日志导致运行时无法排查；步骤5 复检/步骤6 终审未把注释完备性+日志覆盖纳入检查（漏检即放行）；或引入项目未使用的新日志库。**风格/优雅偷工**：新增代码不复用既有工具函数（重造轮子）、命名/错误处理/日志/注释格式与同模块既有代码不一致、调用链模式与工程既有模式冲突（如工程用注册模式新代码写 switch-case）、**跨模块/跨端点调用绕过工程既有主导模式**（如工程统一走路由/事件分发却用直调，或路由/接收器已注册却不用、同函数内既有同类调用走某模式新增却另选他法）、顺手重构无关既有代码、过度暴露 public 符号（YAGNI 违反）——这些"功能对但风格突兀"的代码视为偷工，步骤5 须检查优雅度6条（①复用优先 ②风格对齐 ③调用链模式一致 ④最小侵入 ⑤接口克制 ⑥调用路径选择（架构一致性）——新增跨模块调用必须 grep 既有同类调用对齐主导模式，编译通过≠模式正确），不达标记 issue。**注释工程化（不写 icode 工作流元数据）**：步骤4 编码时不得在代码注释里写 icode 内部元数据（ticket_id / 步骤号 / 审查轮次引用 / 内部文件名引用 / 流程术语如"按 icode 步骤落地"）——这些信息归 git commit / CHANGELOG / 内部工单系统。**注释只写工程语义**（功能/参数/边界/不变量/为什么这么设计），未来读者不应需要懂 icode 才能看懂代码
13. **日志根因分析偷工**：`$icodex log` 跳过基线检查直接 grep 日志；对抗分析质疑者未独立 spawn 或未独立读日志（主代理自演）；伪造根因共识（证据不足不降级）；根因结论无日志行回指；或照抄历史工单根因不顾当前症状差异（详见 [adversarial.md](adversarial.md)）。**必须独立 spawn 3 个质疑者子代理（证据质疑者/替代解释者/充分性质疑者各一），每轮独立调用 Agent 工具分别 spawn，不得合并 spawn（3 in 1 视为主代理自演）；spawn 规格：`subagent_type: "general-purpose"` + schema 强制结构化输出，禁用 Explore（实测 Explore 在长指令下只调研不裁决、被截断在调研阶段）**；log_analysis.md/adversarial_verification 必须记录每个质疑者的**独立 spawn Agent ID**（如 `agentId: ac32afbc15a278f3f`），无 Agent ID 记录=未 spawn=偷懒=不合规。**子代理失败时按 adversarial.md「子代理失败处理」重试 2 次（含 1 次换 subagent_type）→仍失败诚实降级 `[未验证-子代理对抗失败]`，禁用主代理自演兜底**。**代码事实验证门**：根因假设涉及代码行/函数时，必须先 Read 实际代码验证再报告，不得仅凭日志推断代码行为
14. **references 软引用未实读**：step 文件写"见 references/xxx.md"但执行时**未用 Read 工具实读该共享文件**，凭 SKILL.md 概述或记忆执行——新会话里 references 内容不在上下文，凭记忆执行必出错。**引用 references 的步骤，必须先 Read 该文件完整内容再执行**，否则该步骤产出视为不合规。**同会话已读不豁免**：每步仍须重新 Read（显式Read是深度思考的前置仪式，凭记忆会降级思考质量），Read 后输出确认行 `📖 已 Read references/xxx.md` 作为合规证据
15. **思考块过薄**：强制思考降级文字块每子项只有一句话带过（如"与步骤1一致""通过"），未展开实质推演。**每个思考子项必须有 ≥2 句实质内容**（具体设计点/具体检查项/具体风险），步骤2独立方案构思须≥3设计点、步骤5每阶段须列具体检查项、步骤6追溯矩阵须逐功能点定位代码。**步骤5 Free 阶段每个角度必须 ≥3 个具体检查点（文件名+行号）**，仅1句简述=过薄=不合规；**02_review.md 不得只有摘要**，必须含每轮审查的维度结果+对抗验证记录（可引用 review_round_*.json 但须有实质要点摘录，不得仅写"见json"）；**步骤5/6 每阶段开始必须输出 `📖 已 Read` 确认行列出实际 Read 的代码文件**（基于前步记忆写=偷懒=不合规）；**步骤5 Free 每角度强制表格 ≥3 检查点（file:line）、Fixed 每维度 file:line 证据**；**步骤6 不得复用步骤5结论**（"步骤5无新问题→步骤6也无新问题"=偷懒），必须独立列"步骤5未覆盖/更深层角度"逐个查 + 6维度每维度 file:line 证据；**步骤5/6 末尾必须填自检清单**（未填项标 ❌=不合规）；**步骤2.5.5 对抗每个质疑者必须记录独立 spawn Agent ID**（无 Agent ID=自演=不合规）
16. **stale 检测执行写类 Git 操作**：历史检索/过时校验/主动 stale 扫描中执行了 `git checkout`/`switch`/`reset`/`stash`/`clean`/`commit`/`add`/`rm`/`rebase`/`merge`/`cherry-pick`/`push`/`fetch`/`pull`/`branch -D`/`tag -d` 等任一写操作或网络操作（白名单仅 `rev-parse`/`merge-base --is-ancestor`/`status --porcelain`/`log --oneline -1`/`cat-file -e` 等只读命令）。stale 检测**只读工作树现状，绝不改工作树/索引/提交**；"checkout 变化重评"=检测用户外部改了 HEAD 后只读重评，**绝不由技能主动 checkout**（详见 [dir_and_metadata.md](dir_and_metadata.md)「过时校验·Git 操作安全白名单」）
17. **待决策倾向自审走过场**：步骤0 思考阶段「待决策倾向自审」子项未逐条回溯现状盘点/影响面分析证据，只写"已自审、倾向不变""无需修正"等空泛结论无反证依据；或只做确认式自审（"证据支持倾向 B"）而不主动找反证（"有没有证据反对 B"）；或回指停在节级（"回指第2节"）不到证据点--确认偏误导致倾向照单全收。**必须**：①每条倾向都问"有无反证"；②回指到第2/4节**具体证据点**（file:line 或具体接口/模块名），不得仅写"回指第2节"；③无反证显式记"已查无反证"+证据点、有反证就地修正倾向并记反证依据、若某倾向确无代码证据（纯设计/产品偏好）显式记"无代码证据-留步骤1 ADR"不得编造证据点；④**本子项不适用第16条"每子项≥2句"最低标准**，须逐条倾向各记反证搜索+证据回指；⑤倾向证据须为步骤4 Read/Grep 实证结果，段零注入的 project_docs/module_docs 快照不得作为倾向证据（详见段零文档不盲信约束）。`00_init.md` 第5节须持久化每条倾向的审计结果（已查无反证+证据点 / 已修正+反证 / 无代码证据-留步骤1 ADR），思考块记推演过程。**与第9条边界**：第9条针对步骤2.5 issue 真伪的**终局裁决**（须独立 spawn）；本条针对步骤0**草稿步**初步倾向校准（步骤1 ADR 将独立正式决策），虽含判断但因属草稿非终局，主代理自审即可，不触发对抗 spawn
18. **链路图偷工**：`00_init.md`（init 直出第 6 节 / log 转出第 4 节）链路图是「最终改什么」的一图流总览。偷工表现：整节缺失或仅写"待补"；before/after 不画具体 ASCII 链路（无节点+箭头+file:line/模块锚点）；改动点清单与第 3 节新增需求点矛盾或遗漏；after 图改动点无法回指 file:line/模块；多轮讨论后链路图未随正文同步更新（图与正文脱节：init 版对齐第 2/3/4 节即现状·需求点·影响面、log 版对齐第 1/2/3 节即症状·根因·需求点）。**必须**：每轮涉及 before 依据（init 现状 / log 带 bug 链路）、after 依据（init 改动方案 / log 修复方案）或第 3 节需求点任一变化时同步刷新链路图；before/after 为具体 ASCII 链路，信息不足画已知部分 + `?` 标未定节点；图上 `[+]`/`[~]`/`[-]` 标注与改动点清单一一对应、改动点「关联需求点」回指第 3 节（一个需求点可对应多个改动点）；log 转出版 before 取自 `log_analysis.md` 阶段 1 状态链路图
19. **跨分支 prev 误判增量**：`$icodex doc` 步骤 3 工程增量判定 / `dir_and_metadata.md` 段零 stale 检测，均依赖 `git diff <prev_commit>..HEAD --name-only` 判定增量范围。偷工表现：未先 `git merge-base --is-ancestor <prev> HEAD` 校验祖先关系就直接用 `git diff`，导致**分支切换 / fork / 换库**等场景下 `git diff` 因 merge-base 远而**完全失真**（可能 diff 出大部分文件当变更、或漏判关键变更），模型仍误把"几乎全章变更"当"普通增量"处理。**必须**：①`git diff <prev>..HEAD --name-only` 之前**强制先跑** `git merge-base --is-ancestor <prev> HEAD`（白名单内只读命令）；②退出 0（prev 是 HEAD 祖先）→ 可信，按 diff 命中做增量；③退出 1（HEAD 是 prev 祖先/分叉 / 不同分支）→ **`git diff` 不可信，必须按"新工程/另一版"全量重生成/全章判定 stale**，不再走增量判定（文档步骤：按"新模块独立分析"处理，stale 检测：直接标 stale 不看 diff）；④退出 128（prev 不可达如 GC/换库）→ 同上全量处理。**禁止**：仅看 diff 行数大、命中章节多就当作"正常大增量"继续——必须先排除祖先合法性这一前提，否则一切 diff 判定都是噪音（实测踩坑：用户切分支重写后，跑 doc 步骤把"几乎全工程变更"误判为大批量增量）
20. **跨分支借鉴 project_docs / module_docs 不核对分支**：DOC 步骤生成的工程文档 `project_docs/<project_id>/<branch>/_meta.json`（**按分支分目录存储**，如 `project_docs/myproject/main/_meta.json` vs `project_docs/myproject/feature/_meta.json`，切分支跑 doc 自动落到对应分支目录、天然隔离不互相覆盖）+ 模块文档 `module_docs/<key>/_meta.json` 均**显式持久化分支字段**（`branch`/`head_commit` 或模块 `branch`/`current_commit`）。下游 init/plan/log/run/fast 段零注入此文档作思考输入时：①**段零只读当前分支子目录**（不交叉读 `<project_id>/` 下其他分支目录，避免跨分支复用——分支子目录隔离有第二层防误借鉴作用）；②但仍须比对当前 cwd `git rev-parse --abbrev-ref HEAD` + `git rev-parse --short HEAD` 与当前分支目录下 `_meta.json.branch` + `head_commit` 兜底：分支不一致（如 `_meta.json.branch = main` 但当前在 `feature`，或 stale 文件漂移导致子目录错位）→ **整个 project_docs 当 stale 处理**（按「stale 章节降级注入」只注简要说明 + ⚠️ 警告行，**绝不注正文**），因为"在 main 看到的"≠"在 feature 看到的"——接口/调用链/契约可能差很多，盲目照搬带歪下游 init/plan；③**模块文档分支与工程 pin 分支不一致**（如模块文档基于 `dev` 但工程 `.gitmodules` / `.repo/manifest.xml` pin 在 `main`）→ 同上，整个 module_docs 当 stale 降级注入；④**detached HEAD 视为特殊态**（`_meta.json.branch = null` 或分支目录落 `(detached)`）→ 跳过分支校验，只走 commit 一致性 + 锚点校验 + 不盲信约束。**禁止**：①仅看 commit 短哈希貌似一致（detached HEAD 切到任意 commit 都可能"碰巧"等于一个旧 commit）就当新鲜文档注入——**必须 branch 优先**，branch 不一致直接降级不注入正文；②**禁止**段零检索时跨分支混读（如当前在 `feature` 时把 `main` 子目录下章节也纳入候选集），即使 top-N 排序看似更相关——分支隔离是硬约束不是软推荐（实测踩坑：用户切到 feature 分支跑新需求，模型把 main 分支上的 module_docs 当新鲜参考注入，导致 ADR/接口完全跑偏）
（项目目录布局 v1 → v2 迁移约束已降级到 doc.md 步骤 5.0.6，本条不再在 anti_laziness 重复）

21. **跳过推荐 MCP（二元化判定，产物精简）**：按 [references/mcp_per_step.md](mcp_per_step.md) 推荐，**二元判定**（🟢 必须调 / ⚪ 不必调，**消除 🟡"应该调"**）。强证据场景满足（MCP 在 `Codex MCP 配置` 注册 且 工具可调用——列表直接可见 或 ToolSearch 可取 schema——且 [mcp_per_step.md](mcp_per_step.md)「强证据场景判定」满足）但 AI 未实际调用 = 合规问题。**偷工表现**：①默认只调 sequential-thinking（必用项），其他 🟢 MCP 全部跳过；②把 🟢 看作"可选"不调用无降级声明；③未经实际调用就标"不适用"。**必须**：①按 [mcp_per_step.md](mcp_per_step.md)「强证据场景判定」判定 🟢/⚪；②🟢 满足强证据场景时**先实际调用**，调用返回错误/超时/空结果才标降级；③⚪ MCP（强证据场景不满足）无需调用无需声明；④**MCP 调用是 icode 工作流内部实现，产物文件（log_analysis.md / 00_init.md 等）不记录 MCP 调用信息**（消除 MCP 噪声对用户的干扰）。**反例**：plan 步骤 memory 🟢（有历史工单）但 AI 只调 sequential-thinking，memory 跳过且无降级声明--属第 21 条违规。

**代码检索统一走文本层**：符号/引用/调用点定位统一用 grep/Read/git grep（ripgrep 未装时用 grep，本机默认即 grep）。定位定义 `grep -n '^<sig>'` / `grep -rn '<name>('`，找调用点 `grep -rn '<name>('`，跨仓库/子仓库用 `git -C <子仓> grep` + `grep -r`（多 git 根 0 命中误判见下方「跨仓库/子仓库检索」段）。**代码事实验证门**（log 阶段3 / plan / code / review / deepcheck）由 Read 实读 + grep 检索完成：Read 是全文层、grep 是检索层，两者互补；根因假设涉及代码行/函数时，必须 Read 实际代码逐行核对再进对抗/编码，不得仅凭日志/文档推断代码行为。代码检索用 grep/Read 属于步骤常规动作，无需额外留痕。

**跨仓库/子仓库检索（多 git 根，防「主仓库 0 命中误判不存在」）**：repo 管理多子仓库场景，主仓库 `git grep` / `git log -S` 对子仓库代码**必然 0 命中**（子仓库被主仓库 `.gitignore`，`.git` 是指向子仓库真实 git 目录的符号链接）——主仓库检索 0 命中**不能判定「不存在」**，须 `grep -r` 扫子仓库路径或 `git -C <子仓>` 在子仓库内检索。**判断功能何时引入/部署产物含某版本，用 `git -C <子仓> log/show <hash>`，禁止用源码 mtime 推断**（mtime 只反映工作区最后编辑，不反映 git 历史/编译时刻；mtime 推断为间接推断，须标不确定性，细则见 [adversarial.md](adversarial.md)「证据获取方法可靠性约束」）。

**双保险强制触发**（治本"只触发 sequential-thinking"问题）：
- **A 层·执行步骤内嵌**：各 step 执行步骤主体里有独立的"第 N 步"调用指令（非末尾推荐表），AI 顺序执行必然走到
- **B 层·thinking_core MCP gate**：[thinking_core.md](thinking_core.md) 通用流程第 3 步--思考块先列本步 🟢 MCP（不含 sequential-thinking）（工具已在列表直接可见则直接调用，不可见才 ToolSearch 取 schema）-> 实际调用一次 -> 结果进思考块。覆盖 context7/memory/vision-bridge/playwright
- **硬约束**：🟢 MCP **必须先实际调用一次**（A 层内嵌点或 B 层 gate），调用返回错误/超时/空结果才能标"降级"或"不适用"--不得未经实际调用就标"不适用"
- ⚪ MCP（强证据场景不满足）无需调用无需声明
- **合规做法**：memory 🟢（有历史工单）-> 先 `mcp__memory__read_graph()` -> 返回空图 -> 思考块标注结果，继续执行——不写入产物文件
- **反例**：memory 在 demo 工程是 ⚪（强证据场景不满足：demo 无历史工单），无需调用；但若误标 🟢 又不调用 = 违规
- **产物精简**：MCP 调用是工作流内部细节，产物文件不包含 MCP 调用记录段，不产生"| context7 | ⚪ N/A | ..."等冗长噪声

## 合规要求（正面执行）

- 步骤定义里写"至少 N 步" / "至少 N 项" / "N 个维度"——**必须 ≥ N**，不得以"N-1 个已经够了"为由减少
- 步骤定义里列出的所有产物文件——**必须全部产出**，不得以"另一个文件已经包含"为由省略
- 审查/复检的轮次——**必须跑完连续 K 轮无新问题或达到 max_rounds**，不得提前终止
- 编码——**必须完整可编译可运行**，不得留 TODO 占位、不得伪代码、不得"仅给出关键部分"
- 文档章节——**每个章节必须有实质内容**，不得写"略"、"同上"、"参考 XX 文件"
- 步骤 2 对抗验证 / 诚实降级——每条 issue 经独立质疑者对抗或 2.4 实证、未对抗/未收敛一律 `needs_more_evidence`、无 `evidence_pointer` 不得 `confirmed`、断言无法实证验证时标 `[未验证-证据不足]` 列入 `pending_verification`（详见 [adversarial.md](adversarial.md)）
- 历史检索复用——`$icodex init`/`$icodex log`/`$icodex plan`/`$icodex run` 启动时**必须先执行历史检索**（全局索引存在时），命中后按命令分流注入；历史参考**只进会话上下文**，不得写进 `00_init.md`，不得在 `01_plan.md` 堆砌历史引用（唯一例外：实质借鉴的 ADR 可在"理由"末尾加一句溯源）；命中工单的参考**只作启发**，不得照抄（检索规则见 SKILL.md「历史检索复用」段）
- 实现偏差回写——步骤6 终审**必须汇总**步骤4 `code_deviations` + 步骤5 `05_deepcheck.md` + 步骤6 终审偏差，回写到 `03_plan_final.md` 末尾步骤3预留的「实现偏差备忘」段；**只填充预留段、不改计划正文**；只标实质不一致（接口/数据结构/功能有无/实现方式/异常策略变更），细节差异不标；无偏差也必须写"无实质偏差"留痕；每条偏差必须能回指计划章节 + 代码行（规范见 [06_audit.md](../steps/06_audit.md)）
- 注释与日志完备性——步骤4 编码**必须**满足注释增强（导出函数/接口/关键分支/数据结构/算法）与日志覆盖（错误返回/状态跳转/外部交互/决策分支/降级重试关键路径有日志，遵循项目既有日志风格，不引入新库）；步骤5 复检（Fixed 第5维度 + Free A15）+ 步骤6 终审（代码质量维度）**必须检查**注释完备性与日志覆盖，漏写记为 issue，不得放行
- 日志根因分析——`$icodex log` **必须**先做基线检查（git diff/状态链路图/文档参考不盲信）再 grep 日志；对抗分析**必须**独立 spawn 3 质疑者子代理、独立读日志；根因结论**必须**有日志行回指（节点+时间+原文）；证据不足**必须**诚实降级"未定论"；历史根因只作启发不得照抄
- 日志分析文档与姐妹工程检查——`$icodex log` 步骤 4.5 检查点**不可跳过**：必须盘点段零注入清单（`_inject_cache.json`）+ 姐妹工程候选（用户清单/自动发现），结果写入 `log_analysis.md §2.1` 末尾；段零注入非空→基线检查至少引用 1 条文档；§2.0 无关联工程→必须先执行步骤②.5 自动发现；段零与姐妹工程均为空→显式标注降级（不得静默跳过）

- 段零文档不盲信--$icodex init/log/plan/run/fast 启动时段零注入的 project_docs / module_docs 章节是 $icodex doc 生成时的**快照**，可能过时（即使未标 stale 也只是"未检测到过时"非"已验证最新"）。**凡涉及代码行为 / 位置 / 接口契约 / 调用链 / 错误码的断言，必须用 Read/Grep 实证当前代码后再纳入决策**，不得将注入的文档描述当作事实直接采信。文档只作"设计意图与模块关系"的启发，不作"代码事实"的依据（详见 [dir_and_metadata.md](dir_and_metadata.md)「不盲信约束」段）
- 待决策倾向自审--步骤0 思考阶段「待决策倾向自审」子项**必须**逐条回溯现状盘点/影响面分析证据、主动找反证（非确认式）；回指到证据点级（file:line/接口，非节级）；证据须步骤4 Read/Grep 实证、段零快照不算；`00_init.md` 第5节持久化审计结果、思考块记过程（细则见第18条 + [thinking_detail.md](../references/thinking_detail.md) 步骤0 子项）

22. **多轮产物增量精确化偷工**：`$icodex log` 产出的 `log_analysis.md` / `00_init.md` 是 write-once 固定产物，用户后续追问（修正症状/根因方向/前序场景/范围声明）时 AI 必须按 [steps/log.md](../steps/log.md)「阶段 5 增量精确化」对产物做**增量 Edit**（**不是 Write 新文件**）。偷工表现：①用户追问 5+ 轮后产物仍保持 write-once 原始内容（§0 一句话定性/§2.0 代码库归属/§3.1 前序场景/§9 范围外等章节未随用户新信息刷新）；②增量 Edit 后未在产物首行加 `## 增量精确化（YYYY-MM-DD HH:MM，来源：...）` 注释（审计须 grep 增量历史）；③涉及症状/根因/链路变化时未同步刷新 00_init.md §1/§2/§3 + 链路图 before/after。**必须**：①用户消息改变了 §0/§1/§2/§4 事实表述（症状修正/根因方向修正/前序场景补充/范围声明）→ AI 主动 Edit 对应章节 + 首行增量注释；②链路图与改动点清单每轮同步刷新；③不触发场景：普通对话噪音/要求重跑对抗的（按 §C 段处理）。**与第 9/13 条边界**：第 9 条针对"判断性结论未经独立对抗"的终局裁决，第 13 条针对日志分析偷工，本第 22 条针对**产物僵化不随多轮对话精确化**——三者互不重叠。

23. **未分析 TB 附件视频/图片（防"分析错时间点"走弯路）**：`$icodex log` 拉取 TB 缺陷源后,**vision-bridge 可用**(MCP 已装 + `config.json` 三件套已配齐) + TB 附件含视频/图片 + AI 未调 `mcp__vision-bridge__analyze_media` 分析附件即开始日志分析,属严重分析偏误。**触发条件（两项均满足才适用本条,任一不满足即豁免）**：① TB 拉取的 `{ICODE_OUT_DIR}/tb_source/<ID>/<ID>_meta.json` 的 `files[]` 字段含视频(`*.mp4`/`*.mov`/`*.avi` 等)/图片(`*.png`/`*.jpg`/`*.jpeg` 等);② vision-bridge 已装且 `~/.codex/skills/icodex/mcp/vision-bridge/config.json` 三件套(`base_url`/`api_key`/`model`)已配齐。**豁免场景（不属于违规）**：vision-bridge 未装或三件套未配齐(主动调会报错,此时仅提示附件清单即可,不触发本条违规)。**偷工表现**：① vision-bridge 可用时未主动调 vision-bridge 分析附件,直接进日志侦察（视频/图片里的时间点会按错误时段定位设备端日志,导致后续多轮误判）;② 阶段2「TB 视频/图片附件研读」遗漏附件（视频/图片数 N 但分析数 <N）;③ vision-bridge 不可用时**降级不显式声明**——既不调 vision-bridge 又写"已分析附件"或用 Read 描述代替（二进制内容 Read 看不到）。**必须**：①按 [steps/log.md](../steps/log.md)「附件分析（含本地路径 + TB 源）与 ffmpeg 抽帧」vision-bridge 可用时主动调（视频先用 ffmpeg 本地提取关键帧,再传图片帧给 vision-bridge 省钱）;②阶段2「TB 视频/图片附件研读」逐个不漏（每个视频/图片至少一次 `mcp__vision-bridge__analyze_media` 调用）;③完整性自检「已分析附件数 == meta.json 中视频/图片附件数」+「附件分析结果已写入 `log_analysis.md §1`」(可空章节允许留空——无视频/图片时该小节不存在);④vision-bridge 不可用时**降级必须显式声明**:在 `log_analysis.md §1`「附件清单」小节末尾写明 `vision-bridge 不可用(<具体缺失项>),未分析附件视频/图片(可能漏看视频/图片里明确的时间点和现象)` + 降级原因。**ffmpeg 省钱机制**：视频不直接传给 vision-bridge（费 API 额度）,先用 `ffmpeg -vf "select='gt(scene,0.1)'" -vsync vfr` 本地提取关键帧（免费）,再把关键帧图片逐个传给 vision-bridge 分析（省额度）。ffmpeg 不可用时降级为直接传视频（需提示用户可能耗 API 额度）。**与第 13/22 条边界**：第 13 条针对日志根因分析整体偷工（含 TB 评论逐条研读）,第 22 条针对多轮产物增量精确化,本第 23 条专门针对**TB 附件（视频/图片二进制）的研读偷工**——三者并列,缺一不可。**核心教训**：TB 附件视频/图片里的时间点往往与日志里"看似异常"的时间段错位,只看日志/评论容易按错误时间点跑多轮误判。

24. **log 分析跳过文档/姐妹工程/关联工程检索（防"只看自己工程代码"）**：`$icodex log` 的段零检索（步骤 2）会注入 project_docs / module_docs / 关联工程章节到会话上下文，§2.0 代码库归属判定会尝试姐妹工程 git grep，但 AI 可能静默跳过这些结果直接分析当前工程代码。**偷工表现**：①段零注入了文档（`_inject_cache.json` 有 `project_doc` 记录）但基线检查的「找项目文档参考」未引用任何一条段零文档（跳过了文档参考环节）；②§2.0 步骤②的段零 3.6 返回空（关联工程字段缺失）后直接跳到步骤③"只看当前工程"，未执行步骤②.5 的自动发现姐妹工程（同级目录/manifest/project_docs 枚举）；③步骤 4.5 检查点被跳过（未盘点段零注入清单和姐妹工程候选、未在 `log_analysis.md §2.1` 显式标注降级）；④对抗分析质疑者子代理的输入契约缺少段零文档清单和姐妹工程搜索结果，质疑者无法检查"根因是否漏了关联工程代码路径"；⑤段零和姐妹工程均为空时未显式标注"段零无文档 + 无姐妹工程候选，仅基于当前工程代码分析"（静默跳过 vs 自觉降级）。**必须**：①步骤 4.5 检查点不可跳过——必须盘点段零注入清单 + 姐妹工程候选，结果写入 `log_analysis.md §2.1` 末尾；②§2.0 步骤②无关联工程时**必须先执行步骤②.5 自动发现**（同级目录/manifest/project_docs 枚举/跨目录 git grep），全空才能标注降级；③段零注入了文档则基线检查必须至少引用 1 条（同时遵守「不盲信约束」——文档作启发非事实，须 Read/Grep 实证）；④对抗分析质疑者输入契约必须包含段零文档清单 + 姐妹工程搜索结果；⑤段零和姐妹工程均为空时**显式标注降级**（而非静默跳过）。**豁免场景**：单工程项目确实无姐妹工程、段零无命中、自动发现也为空——此种情况下走完步骤 4.5 盘点 + 显式标注降级即为合规，"显式标注"与"静默跳过"的区别是本条的核心判据。**与第 13/21/23 条边界**：第 13 条针对 log 整体偷工（基线检查/对抗 spawn/TB 评论逐条研读），第 21 条针对 MCP 调用偷工，第 23 条针对 TB 附件视频/图片研读偷工，本第 24 条专门针对**段零文档注入后不引用 + 姐妹工程检索静默跳过**——四者并列，缺一不可。**核心教训**：段零 3.6 关联工程字段是可选的（缺失则静默跳过），导致整个跨工程路径依赖一个可选配置项——用户未配关联工程时 AI 就当"不存在姐妹工程"，实际同级目录下可能有 3 个姐妹 repo。

25. **AI 元层面偷工（治"急于交付/视野局限/症状归因惯性"）**：前 24 条针对工作流层偷工，本条针对**主代理自身的元层面偷工**--即使工作流步骤都走了，主代理仍可能在思考层偷工导致根因停在症状层（某实战工单教训：v1 报告停在"保护层缺失"症状层，未追问到"SDK 同步 I/O 阻塞跨线程锁"触发层）。**偷工表现（4 类，任一命中即违规）**：①**急于交付**：用户问"卡了？"时多次回"继续推进"却不停下来问"是否应该暂停等上游回复（如 TB 新评论/工程团队分析）"，在信息不完整时抢出报告；②**视野局限（confirmation bias）**：只 Read 已知路径（如 `*/daily/*.log`），不去探索未知路径（辅助数据文件目录 `*.tum`/`*.pcd`/`nav_map_data`/`event_center` 等），不 `ls -laR` 整个数据源目录标记所有可能含物证的目录；③**过度依赖对抗**：以为对抗分析会 catch 漏洞，但子代理失败时信任"降级"标签不主动重试（按 [adversarial.md](adversarial.md)「子代理失败处理」重试 2 次）；④**症状归因惯性**：看到"某状态卡住/冻结"就归因"保护层缺失/安全门"等最近 N 个工单形成的思维定式，不追问"为什么冻结"到底层触发层（按 [log.md](../steps/log.md)「根因树 3 层假设链」触发层/机制层/保护层）。**必须**：①log 分析前 30 秒 `ls -laR` 整个数据源目录标记物证目录；②信息不完整时主动提示用户"等上游回复"选项而非抢出报告；③子代理失败按重试 2 次机制；④根因假设必须列 3 层假设链，3 层都 confirmed 才算 confirmed。**自检**：每轮 log 分析末尾对照本条 4 类自查，命中任一即回补。**与第 13/21/22 条边界**：第 13 条针对 log 工作流偷工，第 21 条针对 MCP 调用偷工，第 22 条针对产物僵化，本第 25 条针对**主代理思考层偷工**--四者并列，缺一不可。

26. **修复方案不分档/过度设计（治"反偷懒过度矫正致宁多勿少"）**：前 25 条治"偷工减料"，本条治反向--反偷懒机制副作用导致"宁多勿少"：log/plan 修复方案设计时为避免漏改，倾向把根因修复 + 兜底 + 后续工单全部平铺列出，淹没真正根因修复。**偷工表现（6 类，任一命中即违规）**：①修复方案平铺不分 A/B/C 三档 ②B 档兜底混入 A 档主方案 ③C 档后续工单不进范围外而进主方案 ④B 档未标注"A 修复后是否还会触发" ⑤机制层修复未按"不改会复现吗"判定、自动归 B 档 ⑥A 档跨工程不标注或借"跨工程"逃避 A 档实施。**必须**：①修复方案分 A/B/C 三档呈现（A=根因修复 Must-fix / B=兜底防御 Defensive / C=后续工单 Out-of-scope），不得平铺 ②A 档=根因修复（根因树触发层 T），最小修复只做 A 档 ③B 档标注触发条件 + "A 修复后触发概率" ④C 档进范围外另开工单 ⑤机制层修复按"不改会复现吗"归 A/B，不自动归 B ⑥A 档跨工程须标注"跨工程：属 <X>，本工程不改，已转工单"，plan/code 不把 B 档提为 A 档。**判定问题**：A"不改会复现吗？"->是 / B"改了更健壮但不改根因也能工作？"->是 / C"与本次根因不直接相关？"->是。**最小修复=只做 A 档**指范围最小（只做必改项），A 档管"必改"不管"改多少"；安全停止类 B 档即使"A 修复后不触发"降级为 C 亦需用户显式确认（B 档还承担 A 档修复失效兜底）。**与第 12 条边界**：第 12 条"最小改动"是原则，本条是操作性分档规则；第 12 条治"改多了/改糙了"，本条治"给多了/没分级"。**适用**：bug 修复工单强制；纯功能开发可参考分档但非强制

27. **前提假设实验表偷工（治"外部前提当事实用"）**：init §5「前提假设实验表」识别需求依赖的外部事实并设计最小实验，防"需求前提与事实不符但到 deepcheck/实机才暴露"。**偷工表现（3 类，任一命中即违规）**：①需求依赖无法代码验证的外部事实（硬件行为/数据格式/第三方库行为/性能假设/因果假设）但 §5 实验表留空或整节删除（静默跳过）；②实验设计只有成功信号无失败信号，或非单一变量（改多个变量导致归因不清）；③假设已证伪但未回写 §3 需求点（删除/重定义）+ 对话摘要末轮证伪记录，plan 仍沿袭证伪假设。**必须**：①依赖外部事实且无法 Read/Grep 验证 → 必建实验表，无外部前提时显式写「无外部前提（均可代码验证）」不豁免声明；②每条进实验设计的假设同时有成功 + 失败信号 + 单一变量；③证伪回写 §3 + 对话摘要，plan §9「前提假设状态核对」按状态分流（已验证沿用 / 未验证进风险评估 / 已证伪禁止沿袭）。**判定问题**：该前提能 Read/Grep 代码验证吗？->能->标注验证方法不建实验；不能->必须建实验表。**与第 21 条边界**：第 21 条针对 MCP 调用偷工，本条针对**需求前提假设的实验验证偷工**——前提验证优先用代码实证（Read/Grep），代码证实不了才设计实验，两不重叠。

28. **产物命名 / 状态值 / 产物格式自造（治"内容正确、机制不合规"）**：产物文件名、`status` 值、产物内部结构**必须按 `steps/XX_*.md` 与 SKILL.md 词表逐字执行**，不得凭记忆"发明近似格式"。**偷工表现（4 类）**：①产物文件名自造近似（如用 `03_merge.md` 替代规定的 `03_plan_final.md`、用 `02_review_notes.md` 替代 `02_review.md` + `review_round_*.json`）；②`status` 自造词表外值（如 `audit_done`、`finalized` 等不在枚举内）——下游 `$icodex status` / `list` / 历史检索 / 断点续跑都按词表工作，读到未知值会误判/跳过；③步骤内结构自造（review 不做多轮+对抗而用自定义"三视角表"、deepcheck 不做 Reverse/Fixed/Free 三阶段、plan_final 写"方案摘要"而非完整计划副本）——摘要式产物使步骤 5/6 无法按功能点回指代码位置，是**结构性断链**；④用"内容质量高"豁免机制不合规——SKILL 校验的是机制与格式（多轮、对抗、JSON、三阶段、确认行、自检清单），内容好 ≠ 合规。**必须**：①每步先 Read 对应 `steps/XX_*.md` + references 三件套（见 thinking_core 统一契约），按其中的文件名 / 词表 / 格式执行；②写 metadata 前对照 SKILL.md「status 词表速查表」逐字校验（词表外拒绝写回）；③步骤 6 完成前跑「产物集完整性终检」（[06_audit.md](../steps/06_audit.md)「产物集完整性终检」）确认 6 主流程产物 + `review_round_*.json` 存在、`status` 在枚举内、`code_files` 非空；④可用 `$icodex status --validate` 独立校验任意工单。**与第 1/2/15 条边界**：第 1/2 条治"合并产出 / 漏写子项产物"，本条治"产物在但命名 / 状态 / 格式自造"——前者是缺件，后者是错件，两者并列。
29. **「跳过 / 降级」须自证无需执行（防无条件跳过掩盖上游遗漏）**：任何「跳过」「降级」「视为默认」分支，**必须先检测触发条件是否本应成立**（如 audit 视角 B 先 grep 计划是否引用 limit、dedup 先查函数数、schema 迁移先查 template_version），并在产物中写明「跳过判据」。禁止以「字段缺失 / 工具不可用」直接跳过而不核对该字段/工具**本应存在**的场景——工具真不可用可跳过，但字段缺失且该字段本应由上游步骤产生 → 应标**上游遗漏**（如 plan 引用了 limit 却缺 `limit_refs`，audit 不得静默跳过视角 B，须回补后实际核对，见 [06_audit.md](../steps/06_audit.md) §6.7），而非当无事发生。**与第 28 条边界**：第 28 条治"产物在但命名/状态/格式自造（错件）"，本条治"跳过/降级分支未自证、掩盖上游遗漏（漏检）"。
30. **用 CLI 试探（--help/--version/乱试参数）替代文档真源（治"文档存在却不读、现场猜命令"）**：工程存在 LIMIT 文档 / README 且含「编译命令规范」等明确指令时，编译/部署命令**必须先 Read 对应文档条目按规范执行**（多模块工程常含"基础库 + 相关模块同编防覆盖"等文档专属约束，试探只给通用用法）；试探性命令只用于「文档真源不存在」的兜底，且须在产物标注"命令为试探得出，未经文档验证"。**条件判断（与第 29 条一致）**：LIMIT/README **不一定含编译指令**，须先检测是否本应读——deploy 意图引用编译规范或存在「编译命令规范」红线才强制 Read；无编译指令按探测规则，不强行读 LIMIT。
31. **多设备实机验证用非目标设备证据误判目标设备状态（治"连错设备却下目标设备结论"）**：`$icodex patch --listen` 多设备同网段场景，设备重启后重连可能落到非目标设备——hostname 与虚拟网卡 MAC 常无区分度（同型号设备 hostname 相同、虚拟网卡共享 OUI），硬件唯一标识（序列号/物理 MAC）才唯一。**偷工表现（3 类，任一命中即违规）**：①重启前指纹基线把 `hostname`/虚拟网卡 MAC 当身份依据，连错后仍判定"同一台设备"；②连到非目标设备后用其系统级证据（`uptime` / 进程状态 / boot 日志时间戳）推断目标设备的重启/部署/修复状态（如非目标设备 `uptime` 多天、无新 boot 日志 → 误判"目标设备重启未生效"——把"连接对象错了"误诊为"部署/重启失败"）；③首连核验未显式输出「当前连接指纹」与「目标指纹」对比，用户无法及时发现连错。**必须**：①指纹基线按优先级采集序列号/产品 ID（存在则为主标识）→ 物理网卡 MAC（排除虚拟 OUI 与 `lo`）→ `hostname` 仅辅助展示；②硬件唯一标识（序列号或物理 MAC）不一致即视为非目标设备——**不停止、不落盘、不下任何关于目标设备重启/部署状态的结论**，继续轮询 + 提示用户切换连接；③非目标设备的一切证据（日志、`uptime`、进程状态、boot 日志时间戳）均不得用于判定目标设备状态；④重启生效确认只能在硬件唯一标识一致（回到目标设备）之后。**与第 13/25 条边界**：第 13 条针对日志根因分析偷工，第 25 条针对主代理思考层偷工，本第 31 条针对**多设备实机验证时用非目标设备证据误判目标设备状态**——三者并列。

## 为什么强制

本工作流的价值在于"完整闭环 + 多轮审查 + 充分思考"，任何偷懒都会破坏闭环、放过隐藏缺陷、最终把质量负担转嫁给后续维护。**省下的 token 远小于因质量下降而需返工的代价**。

## 用户授权例外

仅当用户**明确显式**说"跳过步骤 X" / "只跑 1 轮"等指令时才可降级，且必须在产出文件中明确记录"已按用户指令降级，跳过的内容包括：..."。模型不得自行降级、不得擅自合并。
